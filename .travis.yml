sudo: false
language: node_js
node_js:
  - 14
cache:
  directories:
    - node_modules
matrix:
    include:
        - env: TARGET=data-provider
        - env: TARGET=status-web
script: 
  - cd $TARGET
  - npm ci
  - npm run build
  - npm run test
